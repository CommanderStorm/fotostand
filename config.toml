# Fotostand Configuration
# This file is automatically validated on server startup.
# All fields are required unless marked as optional.

[event]
title = "UNITY 2026"
subtitle = "Die Garchinger Sommerparty"

[theme]
background_color = "#04190a"
primary_color = "#40c057"
text_color = "#99e0a6"

[server]
port = 8080
# Upload token hash (SHA-256 in hex format)
# Generate token: openssl rand -hex 64
# Hash it: echo -n "your-token" | openssl dgst -sha256
upload_token_hash = "a5c1804ac843b1c39c9e661cc910a6cccdf55b8504b7917e2de6bb7811d767b1"
# the location where the images in the galery are stored
data_dir = "./data"

[footer]
data_protection_url = "https://www.sv.tum.de/sv/datenschutz/"
imprint_url = "https://www.sv.tum.de/sv/impressum/"
